require 'spec_helper'

describe TypeTracer do
  it 'has a version number' do
    expect(TypeTracer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
